var readline = require('readline');
var inp = readline.createInterface({
input: process.stdin
 });
const userInput = [];
inp.on("line", function()  {
userInput.push(data);
});
inp.on("close", function()  {
console.log(temperature(data)
var temp;
function temperature(data)
{
  
temp = data * (9 / 5) + 32;
var faren = parsefloat(temp.toFixed(2)); 
console.log(temp.toFixed(2));
}
return faren;
}
